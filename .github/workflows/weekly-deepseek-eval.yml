name: Weekly Gateway Interview Evaluation

on:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * 1"

permissions:
  contents: read

jobs:
  synthetic-role-review:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      AI_GATEWAY_API_KEY: ${{ secrets.AI_GATEWAY_API_KEY }}
      AI_PRIMARY_MODEL: deepseek/deepseek-v4-flash
      AI_FALLBACK_MODEL: deepseek/deepseek-v4-pro
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Run non-blocking synthetic role review
        continue-on-error: true
        shell: bash
        run: |
          if [[ -z "$AI_GATEWAY_API_KEY" ]]; then
            echo "AI_GATEWAY_API_KEY is not configured; skipping the weekly model review."
            exit 0
          fi
          npm run eval:interview -- --mode deepseek --output output/weekly-deepseek-eval.json
      - name: Upload evaluation report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: weekly-deepseek-eval
          path: output/weekly-deepseek-eval.json
          if-no-files-found: ignore
          retention-days: 30
